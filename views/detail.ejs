<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/css/card.css" />
  </head>
  <body>
    <section>
      <div class="card">
        <img src="/uploads/<%= data.image %>" alt="image" class="image" />

        <div class="container">
          <span class="product-name"><%= data.place %></span>
          <span class="description"><%= data.description %></span>
          <span><%= data.distance %></span>
          <span><%= data.rate %></span>
        </div>
      </div>
    </section>
  </body>
</html>
